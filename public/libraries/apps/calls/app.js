/* Retired entry point retained for cached portal bundles.
 * Calls live in Communications. Portal.navigation restores legacy Calls URLs
 * directly to Communications > Call lists; no separate sidebar app registers.
 */
