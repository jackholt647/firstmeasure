name: FirstMate platform app
on:
  workflow_dispatch:
  push:
    branches: ['codex/mobile-platform']
    paths: ['native/firstmate/**', 'public/**', '.github/workflows/mobile.yml']
  pull_request:
    paths: ['native/firstmate/**', 'public/**', '.github/workflows/mobile.yml']
permissions:
  contents: read
concurrency:
  group: mobile-${{ github.ref }}
  cancel-in-progress: true
jobs:
  shared:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: public/v1
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: public/v1/package-lock.json
      - run: npm ci
      - run: npm run check
      - run: node --experimental-sqlite --import tsx --test --test-force-exit tests/mobile-api.test.ts tests/phone-features.test.mjs tests/platform-push.test.mjs tests/localization-api.test.ts tests/localization-browser.test.mjs
      - run: node --test tests/mobile-browser.test.mjs
  android:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v4
        with:
          packages: 'platform-tools'
      - run: chmod +x native/firstmate/android/gradlew
      - run: native/firstmate/android/gradlew -p native/firstmate/android assembleDevelopmentDebug testDevelopmentDebugUnitTest bundleProductionRelease
      - name: Enable emulator acceleration
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          script: native/firstmate/android/gradlew -p native/firstmate/android connectedDevelopmentDebugAndroidTest
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: android-builds-and-test-results
          path: |
            native/firstmate/android/app/build/outputs/apk/development/debug/*.apk
            native/firstmate/android/app/build/outputs/bundle/productionRelease/*.aab
            native/firstmate/android/app/build/reports/
  ios:
    runs-on: macos-15
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - run: brew install xcodegen
      - name: Build and test iPhone simulator
        run: bash native/firstmate/scripts/test-ios.sh
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ios-simulator-and-test-results
          path: |
            native/firstmate/ios/build/TestResults-*.xcresult
            native/firstmate/ios/build/DerivedData/Build/Products/Development-iphonesimulator/
