# Assistant dock and app tab — September 24, 2026

Development web release `fd0d9266b1952d4e59b77b386b3fde3dda618184` is active on both serving web nodes (`do-598520065`, `do-603124965`). Source commits `b11630f9ffc5749a8b514e21927dcafb194ea4c2` and `2703b8d331272f3c3cbeb83efd1a5ae55a7fc70a` are pushed to `codex/consolidated-firstmeasure-20260923`.

The first release, `47df07f85869779399b173fc2f1af310aa167119`, overlaid five assistant-related browser files onto exact live baseline `7edfbc71e5c5c17ae1aae4f0add559b4e1532e4b`: `firstmate-apps-manifest.js`, `settings/company.js`, `platform-assistant.js`, `window-manager.js`, and `portal/scripts/core.js`. The follow-up release overlaid only the manifest to clear the inactive assistant tab loading placeholder. Both releases were staged as immutable directories after verifying baseline and target hashes and JavaScript syntax. Each web node was activated separately with readiness checks and a rollback path. No worker, production service, or unrelated portal file was changed.

Local readiness on both nodes and public `https://dev.1m8.ai/v1/health/ready` reported the final release, development data, and enforced outbound isolation. The public manifest returned HTTP 200 with SHA-256 `b06c02de29270ec01828437b2fbfa5fc14394116367bf2d6a315002d75c32d45`. Live Chrome checks confirmed the right dock, full assistant app tab, assistant settings, Minimize back to dock, and an unpinned AI Assistant item in Apps. After the follow-up, the full tab no longer exposed the loading placeholder in the accessibility tree. Frontend assistant tests and the repository check passed before deployment.

Rollback target for the final release on both development web nodes: `47df07f85869779399b173fc2f1af310aa167119`; the earlier baseline is `7edfbc71e5c5c17ae1aae4f0add559b4e1532e4b`. The development autoscale image remains historical; a replacement node needs this release applied to retain the behavior. Production was not changed.
