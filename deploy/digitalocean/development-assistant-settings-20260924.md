# Assistant settings in window — September 24, 2026

Development web release `f22d81a5d937e1627672b5da9f8b3b57e9b4d4da` applies source commit `35607ad3cf940567dab53e3a9cf399ebd1da169d` from `codex/consolidated-firstmeasure-20260923`. The release was staged from the exact live baseline `cefc0f3db9ed0ec02eb69fbc0ebb8e2bc40bcfdd` on both serving web nodes (`do-598520065`, `do-603124965`). It overlays only `public/libraries/platform-assistant/platform-assistant.js` and `public/libraries/apps/settings/company.js`; baseline and target SHA-256 hashes were checked on each node. Target hashes are `9a23743d4f2d6e84a49f1de02a697c8d7cb34c28e4ca21cf5f2402df5a275151` and `80868208f93a7fd28c000505535c4b8e5727281d4e1b6bd1f23c953718a92427`, respectively.

Assistant settings now live inside the assistant in both docked and full-screen layouts. Tabs contain Personalization, Memory, Capabilities, Agents, and Advanced settings. The prior link to company settings was removed. Enabling controls use switches. Company administrators can configure the assistant's capabilities and each registered agent in this view; platform instructions are editable only by verified platform administrators. The existing permission-checked settings APIs remain the authority for saving.

`npm run test:assistant:frontend`, `npm run check`, syntax checks for both changed JavaScript files, and `git diff --check` passed locally. Both nodes passed post-activation local readiness with development data and enforced outbound isolation. The public readiness endpoint reported this release, and public responses for both changed assets matched the target hashes. Live Chrome checks confirmed the five tabs, memory and capability switches, registered agent controls, no company-settings navigation, and persistence of the selected Agents tab when maximizing the assistant. Settings were not modified during browser QA.

Rollback target on both development web nodes: `cefc0f3db9ed0ec02eb69fbc0ebb8e2bc40bcfdd`. Production was not changed. The development autoscale image remains historical; replacement nodes need this release to retain the behavior.
