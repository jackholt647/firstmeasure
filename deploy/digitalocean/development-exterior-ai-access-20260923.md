# AI access and capture boundaries — September 23, 2026

Source release `7a5daa9c0f35ee5a85e393190610e3a218be0a38`, runtime baseline `ff8e94a3a66dc6dea5694705df968e91d4c3e36b`.

The user's two saved Sol runs failed before contacting OpenAI. Identical signed project feedback requests through the public development hostname returned three successes and five generic route-not-found responses in eight attempts. These were routing failures, not a project authorization decision. The existing private web API returned eight successes in eight attempts.

On the development compatibility host, installed `/etc/php/8.3/fpm/pool.d/zz-firstmeasure-development-api.conf` from the adjacent `php-development-api.conf`, after verifying the running service uses development data. PHP configuration validation and reload passed. Project traffic now uses the existing private web API, with the same signatures and authorization. Capability and project feedback checks passed afterward; the authenticated editor's Resources panel again loaded all 20 customer resources. No credentials, permissions or production configuration changed. Public endpoint routing remains a separate infrastructure issue; this correction removes that path from the PHP editor bridge.

AI now distinguishes missing/unavailable project service routes from denied access and displays unique per-run errors above the table. Captures offset surface fills behind their edge lines and overlay visible face-ID boundaries, retaining coplanar face divisions without drawing occluded edges through the building. Saved screenshots remain unchanged; new runs use the corrected capture.

The browser pipeline regression passed, including a 65-pixel continuous coplanar boundary, hidden-face exclusion, both question styles, placement history and visible failure text. PHP lint passed. The two-file delta preserves concurrent soffit and sticker editing changes. Local deployment evidence is in ignored `output/exterior-ai-access-20260923/`.

All three roles activated and passed readiness verification; the public script hash matched the manifest. A live Sol low counts-only request at 3:58:51 PM completed for 25 faces and persisted in history. It returned null counts because the camera had reset to an elevated view after refresh while the reference was the right-side ground photograph. This verifies transport and result handling, not counting accuracy. The new capture was visually inspected and shows continuous face boundaries. The earlier failed runs remain preserved.
