# Mobile report order sequence — September 24, 2026

Development web release `63db2b3989acf39b5bcd3c720aabe33703edd565` is active on both serving nodes. It was built from the prior `7769ec15fd6f60ddf14336041b9a85046d4e4263` runtime through the two narrow follow-up releases `99df5a1a18d274cd2dea1d5e7a5d8fe2fa7f6119` and `eb4dd8e4cd49d2b563e27f7cd9e90750ccf3aa1e`. Only the project request, project map, FirstMeasure order markup, and Full Structure order scripts changed in the runtime. Existing release files, private assets, environment configuration, and data were preserved.

The mobile New Report location page starts with an address field. Choosing a Google address reveals Residential, Commercial, and Multi-Family. Selecting a type condenses it into a dropdown after a short transition and reveals Roof Only and, when available, Full Structure. Pin confirmation stays hidden until scope is selected. Confirming pins condenses the control to “Pin confirmed” and reveals a large inline Next button. The fixed bottom Next is absent on Location. Unconfirming or adjusting pins hides inline Next; Back from Details animates to Location. Desktop behavior remains unchanged. The final CSS also hides the map pin hint before scope selection.

The location layout needed a live follow-up: its address and type controls had been moved inside the customer section, while existing mobile CSS hid that section. The `eb4dd8e` source correction hides customer fields individually so the address and type controls remain visible on Location.

JavaScript syntax, Git whitespace, and 15 focused mobile and exterior checks passed. Chrome at a mobile breakpoint confirmed the Google address dropdown gate, type and scope sequence, Roof Only and Full Structure pin controls, inline Next, Details transition, Back transition, and loss of Next when pin confirmation is reversed. Both web nodes passed exact-release local readiness with development isolation. The public project request script SHA-256 is `5102c9d74c970279c86d6915f37af48c46e00a629be1304aa6673a77d2a254c4`; public readiness returned the same release. The test browser viewport was restored. No order was submitted.

Rollback target on both development web nodes: `eb4dd8e4cd49d2b563e27f7cd9e90750ccf3aa1e` for the final hint-only change, or `7769ec15fd6f60ddf14336041b9a85046d4e4263` for the full sequence. Production and the historical development autoscale image were unchanged.
