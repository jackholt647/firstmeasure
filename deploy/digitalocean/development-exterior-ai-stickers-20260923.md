# AI sticker count experiment — September 23, 2026

Initial release `95cce15f0a25f4c0396ddecd3a48db750d0e2edc` added six runtime files over `794ef7c9f31483048bd63ec77cf2fa1d025caf1e`. Prompt follow-up `55e5497f9305219e6fd7e1c35b82f2e5f7457f90` clarifies the question wording with a one-file PHP delta over `900b2158b332e5e4698fc72c46c7dd44b4386d55`, retaining the concurrent nudge improvements. The final highlight release `b7b759cf7d848b9e5dc1dfccad2593ebc8026bf9` changes only wall_mode.js over `ad5c325fc4406ed4db71c56f2e7ef3037cb1a073`, preserving photo wheel zoom. Clicking a row switches to opaque mode so its selected face remains visible. All three development roles passed readiness and isolation checks. Public JavaScript hashes match. Production is unchanged.

The narrow AI panel now has Rotation and Stickers sub-tabs. Stickers offers Luna, Sol and Astra; supported thinking efforts; a single all-face request or four concurrent per-face workers; the current camera, rotation result or eight saved angles; and a rotation, project or uploaded photograph. It captures opaque geometry using a separate renderer. An ID buffer excludes occluded faces and tiny projections under 64 pixels, retains holes, and numbers the visible wall faces. Per-face requests highlight only their requested face. The original editor camera is retained for Current camera. No actual window/door stickers are created.

A compact W/D/G table selects a single corresponding face when its row is clicked. Geometry signatures reject stale face references. Inputs, captured poses, highlights, results, complete API responses, errors and available reasoning summaries are retained in per-project IndexedDB history. Inputs/evidence and JSON export stay collapsed. Failed requests remain distinct from unknown counts (null) and zero. Refresh restores past runs. The main actions are Run and Stop.

The existing owner-only development endpoint continues to enforce host, owner, origin and project authorization. Model and effort allowlists, image bounds and exact face-result validation apply. A shared 120-request/minute ceiling allows the per-face experiment; keys remain server-private. The helper rejects direct access. Full successful model responses are logged in the Chrome console; no private chain of thought is requested or exposed.

Validation: 67 focused checks passed (rotation, opaque visibility/camera preservation, input configuration, response validation, per-face images, partial failure, selection identity and history reload), plus PHP lint for editor and both endpoints. The committed wall-mode source independently passed its 62 checks, excluding concurrent uncommitted nudge tests. Both endpoints returned 404 without owner authorization.

Live Luna/low/all-face run at 14:06 local time completed 34 faces, returning six windows, one pedestrian door and two garage doors in total. These counts are experiment output, not verified ground truth. A preceding attempt overlapped a development service restart and is retained as a failed run. Live row selection was visually verified for faces 8 and 17; selecting 17 removed the highlight from 8. The final opaque-selection change passed all 63 wall-mode checks. Sol and Astra configuration is implemented but was not separately exercised against the live API.

Evidence: `output/sticker-test-results.txt`, `output/sticker-committed-tests.txt`, and `output/exterior-ai-stickers-20260923/` , `output/exterior-ai-stickers-final-20260923/` and `output/exterior-ai-stickers-highlight-20260923/` staging/activation/verification records.
