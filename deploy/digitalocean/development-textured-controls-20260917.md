# Development Textured mode, grade and Graphics controls

- Runtime: `3e4d25e52b2a569ffbc9081a8c9705ae642b5f0d`.
- Baseline: `397ab55d6af2ef881c962be629d8223af3266100`.
- Branch: `codex/exterior-trim-controls`, isolated FirstMeasure Trim worktree. Concurrent report, opening-trim, base-geometry and other shared-checkout work was preserved and excluded from the release.
- The normal display cycle is now Translucent, Opaque, Textured. Textured uses the PBR presentation renderer. Saved Rendered preferences migrate to Textured. The former texture renderer remains as `match-textured`, labeled Match textured, and is excluded from the normal cycle. Developers can opt into it with `window.FIRSTMEASURE_MATCH_TEXTURED = true`.
- Removed the synthetic studio ground. The renderer now clones the visible grade mesh, preserving actual elevations, triangles and slope. Grade off means no ground surface. Base renders independently as plain concrete, including the base-only view; polygon offsets resolve coplanar base/grade depth ties without changing measured geometry. Hidden ancestors are respected.
- Graphics appears immediately after Advanced Settings in the main header, only while the PBR Textured presentation is active. Its dropdown starts closed, stays open during scene redraws, closes on Escape/outside clicks and resets closed on leaving/re-entering the mode. Lighting, sun, exposure and Save 4K are inside it. Graphics interactions are excluded from model pointer and keyboard handling.
- Validation: 825 tests passed in the isolated release checkout; 64 focused tests passed in the shared checkout; 816 non-browser tests passed on Linux. The real-WebGL fixture checks sloped grade vertices and topology, independent base/grade visibility, no synthetic ground, concrete without texture maps, depth priority, menu placement/lifecycle, 4K export and renderer/model restoration. Mode tests check migration, default cycling, hidden legacy opt-in and PBR activation.
- Manual local browser checks on the saved house: grade on/off, base-only view, dropdown placement and opening, mode exit/re-entry. No browser errors. Layer visibility restored to its starting state afterward; geometry was not edited.
- Artifact: `/home/dev/exteriors-3e4d25e.tar.gz`, 284772964 bytes, SHA-256 `c19f320e6c087924831cc836baf8e88dd7195c668f6a938c4568bdfef9d3ccd9`.
- Public delta: exterior_rendered.js, exterior_toolbar.js, scene_3d.js, wall_editor.js, wall_face_draft.js and wall_mode.js. Assets and dependencies are unchanged.
- All three development roles staged with 1352 unchanged public files verified, then activated successfully. Public readiness reports the new release with outbound isolation enforced; all six served JavaScript modules match tested Git source after newline normalization. Production and experimental access settings are unchanged.
